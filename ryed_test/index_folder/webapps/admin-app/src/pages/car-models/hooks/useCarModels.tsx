// import { useGetCarModelsQuery } from '@/api/carModelsEndpoints';

export const useCarModels = () => {
  // const { data: carModelsData } = useGetCarModelsQuery('');
  // console.log(carModelsData);
};
