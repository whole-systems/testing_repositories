// import { useGetJourneysQuery } from '@/api/journeysEndpoints';
// import { useMemo } from 'react';

export const useJourneys = () => {
  // const { data } = useGetJourneysQuery({ page: 0, limit: 10 });
  // console.log(data);
  // const journeysDataTable = useMemo(() => {
  //   return (
  //     data?.journeys.map((item) => ({
  //       id: item.id,
  //       fromAddress: item.fromAddress,
  //       toAddress: item.toAddress,
  //       createTime: new Date(item.createdAt).toDateString(),
  //       status: item.status,
  //     })) ?? []
  //   );
  // }, [data]);
  // return { data: { journeysDataTable } };
};
